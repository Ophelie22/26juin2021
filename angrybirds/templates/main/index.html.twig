{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Hello MainController!{% endblock %}

{% block body %}

<h1>A Field Guide to Angry Birds</h1>

<h2>Games that lodge in the brain often have comical stars</h2>

<div class="row">

    {# On boucle sur la liste des oiseaux pour tous les afficher #}
{# {{ dump(birds)}}  cela m'indique bien le dump du tableau#}
{#{{ dump(birds[3]['description']) }} #}
 {% for bird in birds %}
        <div class="col-3">
            <div class="card border-primary m-1">
                <div class="card-body">
                    <p class="card-text"><img src="images/{{ bird.image }}" alt=""></p>
                    <h4 class="card-title">{{ bird.name }}</h4>
                </div>
            </div>
        </div>
    {% endfor %}
</div>

{% endblock %}